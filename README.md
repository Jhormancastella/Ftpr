# Ftpr Studio

PWA modular para fotografía con cámara, segmentación de persona mediante MediaPipe, fondos virtuales, captura y galería local.

## Desarrollo

```bash
npm install
npm run dev
```

La cámara requiere `https://` o `localhost`. Para producción: `npm run build` y publicar `dist/`.
