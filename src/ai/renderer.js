import {CONFIG} from '../config.js';import {drawBackground} from './backgroundEngine.js';import {state} from '../state.js';
export function renderFrame(ctx,video,mask){const {outputWidth:w,outputHeight:h}=CONFIG;ctx.clearRect(0,0,w,h);if(mask){ctx.drawImage(mask,0,0,w,h);ctx.globalCompositeOperation='source-in';ctx.drawImage(video,0,0,w,h);ctx.globalCompositeOperation='destination-over';drawBackground(ctx,w,h);ctx.globalCompositeOperation='source-over'}else{drawBackground(ctx,w,h);ctx.globalCompositeOperation='source-over';ctx.globalAlpha=.9;ctx.drawImage(video,0,0,w,h);ctx.globalAlpha=1}state.ai.fps=(state.ai.fps||0)+1}
