import {setColor,setTransparent,setImage} from '../ai/backgroundEngine.js';
export function bindBackgroundPanel(){document.querySelector('#swatches').addEventListener('click',event=>{const button=event.target.closest('.swatch');if(!button)return;document.querySelectorAll('.swatch').forEach(el=>el.classList.remove('active'));button.classList.add('active');button.dataset.transparent?setTransparent():setColor(button.dataset.color)});document.querySelector('#bgUpload').addEventListener('change',event=>{const file=event.target.files[0];if(!file)return;const reader=new FileReader();reader.onload=()=>{const image=new Image();image.onload=()=>setImage(image,file.name);image.src=reader.result};reader.readAsDataURL(file)})}
