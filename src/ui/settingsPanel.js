import {applyConstraint} from '../camera/cameraControls.js';import {state} from '../state.js';
export function renderControls(){const caps=state.camera.capabilities||{};const items=[];const add=(key,label,min,max,step,value,unit='')=>{if(!caps[key])return;items.push(`<div class="control-group"><label class="control-label"><span>${label}</span><output id="value-${key}">${value}${unit}</output></label><input type="range" data-control="${key}" min="${caps[key].min}" max="${caps[key].max}" step="${step||caps[key].step||1}" value="${value}"></div>`)};const settings=state.camera.track?.getSettings?.()||{};add('zoom','Zoom',caps.zoom?.min,caps.zoom?.max,caps.zoom?.step,settings.zoom||caps.zoom?.min,'x');add('exposureCompensation','Exposición',caps.exposureCompensation?.min,caps.exposureCompensation?.max,caps.exposureCompensation?.step,settings.exposureCompensation||0);add('colorTemperature','Balance de blancos',caps.colorTemperature?.min,caps.colorTemperature?.max,caps.colorTemperature?.step,settings.colorTemperature||4500,'K');if(caps.torch)items.push('<button class="control-btn" data-toggle="torch">🔦 Flash</button>');if(caps.focusMode)items.push('<button class="control-btn" data-toggle="focusMode">🔍 Enfoque automático</button>');document.querySelector('#controls').innerHTML=items.length?items.join(''):'<p class="muted">Este dispositivo no expone controles avanzados.</p>';document.querySelectorAll('[data-control]').forEach(input=>input.addEventListener('input',async()=>{const value=Number(input.value);const output=document.querySelector(`#value-${input.dataset.control}`);output.textContent=`${value}${input.dataset.control==='zoom'?'x':input.dataset.control==='colorTemperature'?'K':''}`;await applyConstraint(input.dataset.control,value)}));document.querySelectorAll('[data-toggle]').forEach(button=>button.addEventListener('click',async()=>{const key=button.dataset.toggle;const value=key==='focusMode'?'continuous':!button.classList.contains('active');if(await applyConstraint(key,value)){button.classList.toggle('active')}}))}
